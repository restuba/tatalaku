// Auth service: all business logic for registration, login, and token refresh.
// Authorization checks (e.g., token validity) live here, not in the controller.
export class AuthService {
  // TODO: implement register(), login(), refreshToken(), logout()
}

export const authService = new AuthService();
